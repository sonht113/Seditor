---
"seditor-vue": none
---

Add Vue 3 bindings package (`seditor-vue`) with `<Editor>`, `<Toolbar>`, `useEditor()`, `ColorPicker`, `FontSizePicker`, and `LinkTooltip` components. Includes v-model support, 42 tests, and a playground demo app.
